import { getNeo4jDriver } from './lib/neo4j'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const driver = getNeo4jDriver(); const session = driver.session(); const res = await session.run('MATCH (b:Battle) RETURN b LIMIT 1'); if (res.records.length > 0) { console.log(Object.keys(res.records[0].get('b').properties)); } else { console.log('No battles'); } await session.close(); await driver.close(); } run().catch(console.error);
